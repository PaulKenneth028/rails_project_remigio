<%= form_with(model: my_blogging_website) do |form| %>
  <% if my_blogging_website.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(my_blogging_website.errors.count, "error") %> prohibited this my_blogging_website from being saved:</h2>

      <ul>
        <% my_blogging_website.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
